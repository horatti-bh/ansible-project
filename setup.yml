- name: Setup roboshop project
  hosts: all
  become: yes
#  roles: IN roles rerunning all tasks so we are going with tag concept
#    - mongodb
#    - redis
  tasks:
    - name: Run mongoDB
      include_role:
        name: mongodb
      tags: [mangodb]

    - name: Run redis
      include_role:
        name: redis
      tags: [redis]

